id = "log-report"
title = "Parse access log into JSON summary"
description = "Given an access log, produce /app/report.json with fields total_requests and unique_ips."
timeout = 300

artifacts = ["/app/report.json"]

[task]
name = "dynamo/log-report"

[metadata]
category = "data_processing_and_etl"
subcategory = "text_processing"
task_objective = ["transform", "generate"]
artifact_type = ["generated_output_artifact"]
expert_time_estimate_hours = 0.3
model_tested = "GPT-5.4"
agent_tested = "Terminus-2"
difficulty_explanation = "Parse a small access log and emit summary stats."
solution_explanation = "Read the log and count total requests and unique IPs."
verification_explanation = "Check the report file contents, not just its existence."

[verifier]
timeout_sec = 120.0

[agent]
timeout_sec = 120.0

[environment]
build_timeout_sec = 600.0
cpus = 1
memory_mb = 2048
storage_mb = 10240
gpus = 0
allow_internet = true
mcp_servers = []
